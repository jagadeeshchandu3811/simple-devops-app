pipeline {
    agent any

    tools {
        nodejs 'node18' // Ensure this matches the Node.js version name configured in Jenkins > Global Tool Configuration
    }

    parameters {
        string(name: 'IMAGE_NAME', defaultValue: 'jagadeeshchandu3811/simple-devops-node-app:latest', description: 'Docker Hub image name (e.g., username/app-name:tag)')
    }

    environment {
        DOCKER_CREDENTIALS_ID = 'dockerhub-credentials-id' // Replace with your actual Docker Hub credentials ID from Jenkins credentials
    }

    stages {
        stage('Checkout') {
            steps {
                git branch: 'main', url: 'https://github.com/jagadeeshchandu3811/simple-devops-app.git'
            }
        }

        stage('Install Dependencies') {
            steps {
                sh 'npm install'
            }
        }

        stage('Test') {
            steps {
                sh 'npm test || echo "Tests skipped or failed (for now)"'
            }
        }

        stage('Build Docker Image') {
            steps {
                script {
                    docker.build("${params.IMAGE_NAME}")
                }
            }
        }

        stage('Push to Docker Hub') {
            steps {
                withCredentials([usernamePassword(credentialsId: "${env.DOCKER_CREDENTIALS_ID}", usernameVariable: 'DOCKER_USER', passwordVariable: 'DOCKER_PASS')]) {
                    sh '''
                        echo "$DOCKER_PASS" | docker login -u "$DOCKER_USER" --password-stdin
                        docker push ${IMAGE_NAME}
                    '''
                }
            }
        }
    }
}
